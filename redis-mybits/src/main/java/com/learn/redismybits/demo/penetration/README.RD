缓存穿透：不存在的key数据库查询：
解决：1、布隆过滤器 2、不存在也设置key值，过期时间设置较短，比如5分钟


缓存击穿：单个key时间到期，失效
解决：
	1、互斥锁 (mutex key)
	2、时间永远不过期
	4、采用netflix的hystrix
	
	
缓存雪蹦：同一个时间点大量key失效：
解决：
	1、互斥锁 (mutex key)
	2、时间永远不过期
	3、避免设置相近的过期时间：1、每个过期时间后再加上一个随机过期，
						2、合理规划过期时间，避免出现同一时间大批量到期
	4、采用netflix的hystrix